#include <stdio.h>
#include <stdlib.h>

int findTemplateInMatrix(char **mat, int rows, int columns, char **mask, int maskRows, int maskColumns) {
    int i, j, q, t;
    for (i = 0; i < rows; i++) {
        for (j = 0; j < columns; j++) {
            if (mat[i][j] == mask[0][0]) {
                for (q = 0; q < maskRows; q++) {
                    

                    }
                }
            }
        }
    }

    return 0;
}